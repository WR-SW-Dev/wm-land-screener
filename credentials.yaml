# WM Land Screener — user credentials
# !! Keep this file off GitHub — it is listed in .gitignore !!
#
# To add or update a user, run:
#   python manage_users.py
#
# Passwords are stored as bcrypt hashes — never store plain text here.

credentials:
  usernames:
    sadiebrooks:
      first_name: Sadie
      last_name: Brooks
      email: ""
      password: ""        # run manage_users.py to set
      role: admin

cookie:
  name: wm_screener_auth
  key: wm_screener_secret_key_change_me   # change this to any long random string
  expiry_days: 30
